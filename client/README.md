Client notes

- Start: `npm run dev` (runs Vite at http://localhost:5173)
- Tailwind is preconfigured; edit components in `src` for styling.
- The client expects backend API at `/api/...` — when running locally proxy via a dev server or CORS from backend.
